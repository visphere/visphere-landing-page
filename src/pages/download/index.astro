---
/*
 * Copyright (c) 2023 by MoonSphere Systems
 * Originally developed by Miłosz Gilga <https://miloszgilga.pl>
 */
import { PageHeaderVariant } from '~/ax-ssr/components/Header.astro';
import { useTranslationsSSR } from '~/i18n/translations';
import { i18nHref } from '~/i18n/url-parser';
import { getSafetyLocaleSSR } from '~/i18n/utils';
import MainLayout from '~/layouts/main-layout.astro';

const { lang } = getSafetyLocaleSSR(Astro.url);
const t = useTranslationsSSR('msph.landingPage.download.pageContent');

const headerVariant: PageHeaderVariant = {
  background: 'bg-msph-accent-1',
  foreground: 'text-msph-dark-900',
  signUpButton: {
    fg: 'text-msph-dark-900',
    border: 'border-msph-dark-900',
  },
  openAppButton: {
    bg: 'bg-msph-dark-900',
    fg: 'text-msph-light-100',
    border: 'border-msph-dark-900',
  },
  fontWeight: 'font-medium',
  langPickedTheme: 'light',
};
---

<MainLayout i18nPageId="download" headerVariant={headerVariant}>
  <div slot="headerContent">HEADER CONTENT</div>
  <div slot="mainContent">
    <h1>DOWNLOAD PAGE</h1>
    <div class="my-3">
      <b>{t('testProperty')}</b>
    </div>
    <a href={i18nHref('/', lang)}>GO TO ROOT</a>
  </div>
</MainLayout>
